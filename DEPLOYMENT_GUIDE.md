# 🚀 网站部署指南

## 📋 部署前准备

### 1. 图片准备
将您的齐白石作品图片按照以下规范准备：

#### 📁 文件夹结构
```
public/images/
├── artworks/     # 藏品图片 (8张)
├── hero/         # 轮播图片 (3张)
└── stories/      # 故事配图 (3张)
```

#### 🖼️ 图片规格要求
- **格式**：JPG 或 PNG
- **藏品图片**：建议 800x600px 或更高
- **轮播图片**：建议 1600x900px 或更高
- **故事配图**：建议 800x600px 或更高
- **文件大小**：每张图片建议不超过 2MB

#### 📝 文件命名
```
artworks/
├── artwork-001.jpg  # 群虾戏水图
├── artwork-002.jpg  # 墨荷图
├── artwork-003.jpg  # 白菜蚱蜢
├── artwork-004.jpg  # 山水十二条屏
├── artwork-005.jpg  # 桃花流水鳜鱼肥
├── artwork-006.jpg  # 牡丹蜜蜂
├── artwork-007.jpg  # 青花缠枝莲纹瓶
└── artwork-008.jpg  # 端溪老坑砚

hero/
├── hero-001.jpg     # 齐门虾趣
├── hero-002.jpg     # 荷塘清韵
└── hero-003.jpg     # 山水意境

stories/
├── story-001.jpg    # 齐门画虾之秘
├── story-002.jpg    # 一方端砚的故事
└── story-003.jpg    # 荷花情缘
```

## 🌐 部署选项

### 选项 1: Vercel 部署（推荐）
```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 登录 Vercel
vercel login

# 3. 部署
vercel

# 4. 生产环境部署
vercel --prod
```

### 选项 2: Netlify 部署
```bash
# 1. 构建项目
npm run build

# 2. 将 dist 文件夹拖拽到 Netlify 部署页面
# 或使用 Netlify CLI
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

### 选项 3: 传统服务器部署
```bash
# 1. 构建项目
npm run build

# 2. 将 dist 文件夹内容上传到服务器
# 3. 配置 Nginx 或 Apache
```

## 🔧 Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/your/dist;
    index index.html;

    # 处理 SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 图片缓存
    location ~* \.(jpg|jpeg|png|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 静态资源缓存
    location ~* \.(css|js)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 📊 性能优化建议

### 1. 图片优化
```bash
# 使用 imagemin 压缩图片
npm install -g imagemin-cli imagemin-mozjpeg imagemin-pngquant

# 压缩 JPG
imagemin public/images/**/*.jpg --out-dir=public/images/optimized --plugin=mozjpeg

# 压缩 PNG
imagemin public/images/**/*.png --out-dir=public/images/optimized --plugin=pngquant
```

### 2. 启用 CDN
- 将图片上传到 CDN（如阿里云 OSS、腾讯云 COS）
- 修改 `src/config/images.ts` 中的路径为 CDN 地址

### 3. 启用 GZIP 压缩
在服务器配置中启用 GZIP 压缩以减少传输大小。

## 🔍 部署后检查清单

- [ ] 所有图片正常显示
- [ ] 轮播图片自动切换
- [ ] 藏品画廊交互正常
- [ ] 响应式布局在移动设备上正常
- [ ] 自定义鼠标光标正常显示
- [ ] 页面加载速度合理（< 3秒）
- [ ] SEO 元标签正确设置

## 🐛 常见问题解决

### 图片不显示
1. 检查图片文件是否存在于 `public/images/` 目录
2. 确认文件名大小写正确
3. 检查图片格式是否支持

### 部署后样式异常
1. 清除浏览器缓存
2. 检查 CSS 文件是否正确加载
3. 确认字体文件路径正确

### 移动端显示问题
1. 检查响应式断点设置
2. 测试不同设备尺寸
3. 确认触摸交互正常

## 📞 技术支持

如果在部署过程中遇到问题，请检查：
1. Node.js 版本是否兼容（推荐 18+）
2. 依赖包是否正确安装
3. 构建过程是否有错误信息
